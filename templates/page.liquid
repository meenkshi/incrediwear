{% render 'shogun-products', content: page %}
<div class="Container">
  <header class="PageHeader">
    <div class="SectionHeader SectionHeader--center">
      {% if true %} {% include 'bss-title-store-locator' %} {% include 'bss-description-store-locator' %} {% else %} <h1 class="SectionHeader__Heading Heading u-h1">{{ page.title }}</h1> {% endif %}
    </div>
  </header>

  <div class="PageContent PageContent--narrow Rte">
    {{ page.content }}
  </div>
</div>
